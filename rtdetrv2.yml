name: rtdetrv2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cu118
      - torch==2.7.1
      - torchvision==0.22.1
      - wandb
      - -e .
variables:
  GPUS: 4
  SEED: 2025
  # surpress the warning
  NO_ALBUMENTATIONS_UPDATE: 1
