__include__: [
    # clear ver.
    # "../../common/dataset/uav_eagle_detection.yml",
    # degraded ver.
    "../../common/dataset/uav_eagle_degraded_detection.yml",
    "../../common/runtime.yml",
    "../../common/robust_dataloader.yml",
    "../../common/models/robust_rtdetrv2_r34vd.yml",
  ]

output_dir: ./output/rtdetrv2_r34vd/dut_anti_uav/uav_eagle/robust_rtdetrv2_r34vd_120e_1e-4_lr_gdip_multi_level_fine_tuning_no_color_augs_in_clear_degraded

use_ema: True

RobustRTDETR:
  # robust_image_module: DENet
  # robust_image_module: DIP
  robust_image_module: GDIP
  robust_module: ~

# DENet:
#   # original code bug: mm1 used for all 4 branches
#   # compat_mode=True => their config in source code
#   # compat_mode=False => their config in paper
#   compat_mode: false

GDIP:
  multi_level: true
